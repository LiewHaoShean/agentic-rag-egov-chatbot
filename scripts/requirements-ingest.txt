# Ingestion / scraper dependencies ONLY.
# Kept separate from the API runtime (requirements.txt) so the deployed
# FastAPI/Celery service stays lightweight (no Playwright, no scraping stack).
#
# Install into the same venv when you need to (re)populate the DB:
#   pip install -r scripts/requirements-ingest.txt
#   playwright install chromium

# --- scraping stack ---
playwright==1.49.1
beautifulsoup4==4.12.3

# --- PDF ingestion ---
pypdf==5.1.0            # extract embedded text layer
PyMuPDF==1.25.1         # render image-only pages to PNG for Qwen3-VL OCR fallback
langchain-text-splitters==0.3.4   # RecursiveCharacterTextSplitter (also in API reqs)
langchain-core==0.3.28
langdetect==1.0.9
deep-translator==1.11.4

# --- shared runtime deps the scraper imports via core/ + services/ ---
# (so the scraper can be installed standalone without the full API stack)
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
supabase==2.11.0
