# ---- Secrets and environment ----
.env

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# ---- OS / editor ----
.DS_Store

# ---- Tooling ----
# scripts/ is deliberately NOT ignored: the appendix cites the ingestion script
# and the report walks through it — scripts/ingest_scrape.py in Section 4.5.9
# and scripts/eval_rag.py in Section 5.2.3.

# ---- Tests ----
# Excluded at the author's request. Note that Section 5.3.1 reports this suite
# case by case, so the repository will not contain the tests the report cites.
tests/

# ---- Working notes ----
# Internal task list, not part of the deliverable.
TODO.md

# ---- Runtime artefacts ----
callback_dlq.log
callback_dlq.jsonl
previews/

# ---- Report and write-up (not source) ----
# The Word document is ~23 MB and changes on every save, which would bloat the
# history with binary diffs no one can read.
FYPReport.docx
~$*.docx
*.docx

# Report prose drafted alongside the code. Useful working files, but they belong
# to the document rather than to the system.
report-*.md
appendix-*.txt

# ---- Evaluation output ----
# Regenerated by scripts/eval_rag.py. The harness and the question set are
# tracked; their output is not, since it is reproducible and the reported
# figures live in the report itself.
eval_*.csv
eval_*.csv.bak
eval_*_review.md
*.eval
